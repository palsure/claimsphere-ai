{
  "name": "claimsphere-ai",
  "version": "1.0.0",
  "description": "AI-powered claim processing system",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev"
  },
  "dependencies": {
    "next": "^14.0.0"
  }
}

